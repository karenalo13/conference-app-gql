type Category {
  id: ID!
  name: String!
  code: String
}

type Type {
  id: ID!
  name: String!
  code: String
}

type Status {
  id: ID!
  name: String!
  code: String
}

type Country {
  id: ID!
  name: String!
  code: String
}

type County {
  id: ID!
  name: String!
  code: String
}

type City {
  id: ID!
  name: String!
  code: String
}
